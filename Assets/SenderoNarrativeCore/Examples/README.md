# Sendero Narrative Core Examples

This folder is reserved for example assets that demonstrate how to assemble graphs and story contexts. 
Create a new `NarrativeGraph` asset and add `StartNode`, `DialogueNode`, and `CustomEventNode` sub-assets. Then create a `StoryContextAsset` that references the graph.
