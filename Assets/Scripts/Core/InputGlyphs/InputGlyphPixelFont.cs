namespace Core.InputGlyphs
{
    // OBSOLETO — ya no se usa. Ver el comentario en InputGlyphAssetFactory.cs (misma carpeta) para el
    // porqué. La fuente de píxeles a mano que había aquí vive ahora, con el mismo contenido, en
    // Assets/Scripts/Editor/InputGlyphs/InputGlyphPlaceholderFont.cs — solo la usa la herramienta de
    // Editor para generar los placeholders de PlayStation/Switch/Teclado, ya no el juego en tiempo de
    // ejecución.
    //
    // Candidato a borrar sin más (junto con InputGlyphAssetFactory.cs e InputGlyphCanvas.cs) — no lo
    // he tocado más que vaciarlo porque no puedo borrar archivos desde aquí, solo escribirlos. Raúl,
    // puedes eliminar los tres a mano en el Editor cuando quieras: no los usa nada más.
}
